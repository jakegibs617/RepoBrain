# TypeScript API App

The service layer lives in [userService](src/services/userService.ts) and reads
`DATABASE_URL` from the environment.

Adversarial links, deliberately unresolvable:

- [a guide that does not exist](docs/missing-guide.md)
- [an external page](https://example.com/typescript-api-app)
